# Secrets — ne jamais commit
admin-vps.json
embedded_config.json
config.json
packs.reel.json
# les sauvegardes de ces fichiers contiennent les mêmes secrets
*.bak
*.bak-*

# Source claire — seul pack_manager.py (obfusqué) part sur GitHub
pack_manager.source.py

# Scripts admin (pousser_packs.py utilise admin-vps.json ;
# tailles_packs.py importe pack_manager.source.py, non publié)
pousser_packs.py
tailles_packs.py

# Mémoire projet : décrit l'infra du VPS, reste en local
MEMOIRE.md

# Données locales de l'app (dev)
background.gif
packs/
_backups/
state.json

# Build
build/
dist/
dist_installeur/
*.spec
__pycache__/
*.pyc
FiveMPackManager.exe
pm_debug.exe
# regenere par build.py depuis APP_VERSION
version_info.txt
# apercu de controle, pas un asset
app_icon_preview.png

.claude/
